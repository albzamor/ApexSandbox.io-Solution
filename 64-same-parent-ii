public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if (con.AccountId != null && acc.AccountId != null && opp.AccountId != null && acc.AccountId == opp.AccountId && con.AccountId == opp.AccountId)
        return true;
    return false;
}
