public List<Integer> evenNumbers(Integer n) {
    //code here
    List<Integer> listnum = new List<Integer>();
    for (Integer num = 2; num <= 5; num++){
        if (math.mod(num,2) == 0)
            listnum.add(num);
    }
    return listnum;
}