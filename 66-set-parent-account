public void setParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if (con.AccountId != null && acc.Id != null && opp.AccountId != null && acc.Id == opp.AccountId && con.AccountId == opp.AccountId)
        return true;
    return false;
}